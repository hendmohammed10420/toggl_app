<p style="color: green"><%= notice %></p>

  <div class="card-body">
    <%= render @project %>
<div class="d-flex justify-content mt-5 p-2">
  <%= button_to "Edit", edit_project_path(@project), method: :get, class: "btn btn-primary mx-2" %>
  <%= button_to "Back", projects_path, method: :get, class: "btn btn-secondary mx-2" %>
  <%= button_to "Delete", @project, method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-danger mx-2" %>
</div>
  </div>
